{
  isto e um codigo teste,  sintaticamente aceito pelo parser
}

program test (input, output);

  var
  i, j, k; integer;
  x, y, z: real;
  u, v: string;
  p, q: boolean;

  procedure test_proc(x, y: real; var j: integer);

    var
    m,n : integer;
    function fact(n: integer): integer;
      var i: integer;

      begin
        i := 1;
        if(n < 2) then
          fact := 1;
        else
          fact := n * fact(n-1)
      end;

    begin
      write (fact (10))
    end;

  begin
    test_proc
  end.

//        while (i < n) do
//            begin  //fazer um teste sem o begin que é aceito tambem

//implementar sinxtaxe de chamada de função

//trocar para
test_proc(x, y, 3)